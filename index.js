import React from 'react';
import ReactDOM from 'react-dom';
import Application from './src';

const element = document.querySelector('#car-dealership-portal');

ReactDOM.render(<Application />, element);
